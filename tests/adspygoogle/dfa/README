Unit tests to use with the client library. The appropriate unit test should be
executed whenever you make changes to any of the core library's sources.

By default, all unit tests are (and should be) executed against the test
environment.

To run all unit tests, execute "alltests.py" script. The required data files are
located in "data/".
